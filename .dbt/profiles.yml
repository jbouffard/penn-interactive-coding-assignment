default:
  outputs:
    challenge:
      type: postgres
      threads: 3
      host: localhost
      port: 5432
      user: postgres
      pass: password
      dbname: postgres
      schema: public
  target: challenge

