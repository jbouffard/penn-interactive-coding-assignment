version: 2
models:
  - name: points
    description: 'Each Teams Points'
    columns:
      - name: team_name
        description: Team Name
      - name: points
        description: Points total
        tests:
          - greater_than_zero
